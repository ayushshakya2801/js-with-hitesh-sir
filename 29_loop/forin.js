// for in  loop

 const fullform = {
    API : "Appliction Programing interface",
    UPI : "unified payment identify",
    CPU : "Central Processor Unit"

 }
  for (const key in fullform) {
//    console.log(` ${key}, :- ${fullform[key]}`);
 console.log(`${key}, :- ${fullform[key]}`);
 
   
  }